spring:
  application:
    name: board-with-vue-spring
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create
    generate-ddl: true
    show-sql: true
  datasource:
    username: root
    password:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1

logging:
  level:
    web: debug